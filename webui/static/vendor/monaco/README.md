# Monaco Editor

This directory vendors the browser build of Monaco Editor 0.52.2 from the
official `monaco-editor` npm package. The WebUI serves `vs/loader.js` and
the related assets locally, so the assessment editor does not require CDN
access at runtime.

Upstream: https://github.com/microsoft/monaco-editor
License: MIT (see `LICENSE`)
