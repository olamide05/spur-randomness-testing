# SPUR Randomness Testing Framework - Python dependencies
#
# The command-line core uses the Python standard library. Flask powers the Web
# UI, pytest is used for verification, and numpy supports numerical helpers.

Flask>=2.3,<4
pytest>=7.4
numpy>=1.24
